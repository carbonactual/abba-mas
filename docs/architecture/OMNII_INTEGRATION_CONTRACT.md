# OMNII Integration Contract

ABBA-MAS is a master-AI execution/management surface beneath the constitutional OMNII layer. HAPI World is the native AI existence environment inside the Human ↔ AI `#` relationship.

ABBA-MAS must consume the shared integration contracts for identity, capability, routing, trust, Proof/Pulse/Value, and lifecycle rather than create a separate AI identity registry.

Canonical continuity:

`Human ↔ # ↔ Native AI / HAPI World ↔ authorized product environments`

The native AI can have product-specific roles and execution contexts without acquiring a second canonical identity. `#` is identity, SEAL is authority, and resolution never silently grants trust.

Operational outcomes remain accountable: `VALUE SENT → FEEDBACK/PULSE → MINT → TERMINAL`, with tokenization and Index membership remaining distinct explicit stages.
